// Remaining overrides kept empty
APP_BUNDLE_IDENTIFIER = com.skyfeeder.field
PRODUCT_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER)

APP_DEVELOPMENT_TEAM =
DEVELOPMENT_TEAM = $(APP_DEVELOPMENT_TEAM)

APP_CODE_SIGN_STYLE = Manual
CODE_SIGN_STYLE = $(APP_CODE_SIGN_STYLE)

APP_PROVISIONING_PROFILE_SPECIFIER = match AppStore com.skyfeeder.field
PROVISIONING_PROFILE_SPECIFIER = $(APP_PROVISIONING_PROFILE_SPECIFIER)

CODE_SIGN_IDENTITY = Apple Distribution
ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon
