#include "Base.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = com.skyfeeder.field
APP_DISPLAY_NAME = SkyFeeder Field
ALLOW_LOCAL_HTTP = YES
DEFAULT_PROVIDER = PresignedHTTP
CODE_SIGN_STYLE = Automatic
DEVELOPMENT_TEAM = $(TEAM_ID_FALLBACK)
OTHER_SWIFT_FLAGS = $(inherited)
